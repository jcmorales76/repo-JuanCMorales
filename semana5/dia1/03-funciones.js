// ambito gglobal = todo el documento

function calcularAreaTriangulo(base, altura){
    //ponemos el codigo que se va a ejecutar
    //cuando invoquemos / llamemos/ejecutemos
    //esta funcion 
    let area = base*altura/2;

    //a) Indica que va devolver mi funcion
    //b) termina la ejecucion de mi funcion
    return area;

    let resuktado = calcularAreaTriangulo(50, 15);
    let resultado2= calcularAreaTriangulo(100,25);

    console.log(resultado);
    console.log(resultado2);
    consolo.log(calcularAreaTriangulo(10,4));
} //practicar funciones y scope